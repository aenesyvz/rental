import { CardetailPipePipe } from './cardetail-pipe.pipe';

describe('CardetailPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CardetailPipePipe();
    expect(pipe).toBeTruthy();
  });
});
